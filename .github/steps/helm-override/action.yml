name: 'Override Helm Values'
description: 'Override Helm values with image tags for changed services'

inputs:
  commit-sha:
    description: 'Commit SHA for the image tag'
    required: true
  matrix:
    description: 'Matrix configuration'
    required: true
  changes:
    description: 'Changes output'
    required: true
  helm-values-file:
    description: 'Path to the openframe helm values file'
    required: false
    default: 'cli/openframe-helm-values.yaml'
  github-token:
    description: 'GitHub token for repository access'
    required: true
  branch-name:
    description: 'Branch name for deployment'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Override Helm values
      shell: bash
      env:
        COMMIT_SHA: "${{ inputs.commit-sha }}"
        MATRIX: '${{ inputs.matrix }}'
        CHANGES: '${{ inputs.changes }}'
        HELM_VALUES_FILE: '${{ inputs.helm-values-file }}'
      run: |
        set -euo pipefail
        
        # Validate required inputs
        [ -n "$COMMIT_SHA" ] || { echo "Error: COMMIT_SHA is required" >&2; exit 1; }
        [ -n "$MATRIX" ] || { echo "Error: MATRIX is required" >&2; exit 1; }
        [ -n "$CHANGES" ] || { echo "Error: CHANGES is required" >&2; exit 1; }
        [ -n "$HELM_VALUES_FILE" ] || { echo "Error: HELM_VALUES_FILE is required" >&2; exit 1; }

        mkdir -p "$(dirname "$HELM_VALUES_FILE")" && touch "$HELM_VALUES_FILE"

        # Install yq if needed
        if ! command -v yq &> /dev/null; then
          wget -q https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/local/bin/yq
          chmod +x /usr/local/bin/yq
        fi
        
        # Function to set image tags
        set_image_tag() {
          local path="$1"
          if yq eval "$path = \"$COMMIT_SHA\"" -i "$HELM_VALUES_FILE" 2>/dev/null; then
            echo "Updated $path with image tag: $COMMIT_SHA"
          else
            echo "Failed to update $path" >&2
          fi
        }

        echo "Updating Helm values with tag: $COMMIT_SHA"
        # Process each changed service
        jq -c '.[]' <<< "$MATRIX" | while read -r item; do
          name=$(jq -r '.name' <<< "$item")
          changed=$(jq -r --arg name "$name" '.[$name]' <<< "$CHANGES")

          [ "$changed" = "true" ] || continue

          # Add image tag to the app's helm values: openframe-config is a
          # platform-tier Application; every other service lives in the
          # tenant umbrella under openframe-saas.<name without prefix>
          case "$name" in
            openframe-config)
              set_image_tag ".platform.apps.openframe-config.values.image.tag"
              ;;
            openframe-*)
              set_image_tag ".tenant.values.openframe-saas.${name#openframe-}.image.tag"
              ;;
            *)
              echo "No helm values mapping for $name, skipping" >&2
              ;;
          esac

        done

    - name: Override repository passwords and branch
      shell: bash
      env:
        HELM_VALUES_FILE: '${{ inputs.helm-values-file }}'
        GITHUB_TOKEN: '${{ inputs.github-token }}'
        BRANCH_NAME: '${{ inputs.branch-name }}'
      run: |
        set -euo pipefail
        yq eval ".repository.branch = \"$BRANCH_NAME\"" -i "$HELM_VALUES_FILE"
        echo "Updated repository.branch to: $BRANCH_NAME"
        cat "$HELM_VALUES_FILE"
