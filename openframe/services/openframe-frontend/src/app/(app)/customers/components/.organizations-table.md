<!-- source-hash: 914cfa44c9f73fdd6d6b6a8ffbcd395f -->
A React component that renders a paginated, searchable table of organizations with filtering capabilities and navigation to detail/edit pages.

## Key Components

- **OrganizationsTable**: Main component implementing the organizations list view
- **OrganizationNameCell**: Custom cell renderer displaying organization name with optional logo
- **UIOrganizationEntry**: TypeScript interface defining the table row data structure

The component integrates cursor-based pagination, real-time search, client-side filtering by tier/industry, and URL state management for maintaining filter/search state across navigation.

## Usage Example

```typescript
import { OrganizationsTable } from './organizations-table'

// Basic usage in a page component
export default function OrganizationsPage() {
  return (
    <div>
      <OrganizationsTable />
    </div>
  )
}

// The component automatically handles:
// - URL params: ?search=acme&tier[]=enterprise&industry[]=tech
// - Cursor pagination: ?cursor=eyJpZCI6IjEyMyJ9
// - Navigation to /organizations/details/:id on row click
// - Navigation to /organizations/edit/new on add button click
```

The table displays organization name (with logo if enabled), tier/industry, MRR, and last activity. It supports responsive design with columns that hide on smaller screens and includes skeleton loading states during data fetching.

## Empty State

When loading finishes with no error, no active search, and zero customers, the component renders the onboarding empty state via the shared `EmptyState` wrapper (over the library `NoData`) instead of the table — an icon, "No Customers yet" heading, a short description, three feature-highlight blocks (group devices/users, track tickets/SLAs, monitor security), and an "Ask Mingo about Customers" button. A search that returns no matches is intentionally NOT treated as empty: it still falls through to the table body's inline "No customers found. Try adjusting your search." message, so the onboarding state only appears when there is genuinely no data. `EmptyState` centers its content both vertically and horizontally in the content area (`min-h-[60vh] flex items-center justify-center`). In the empty/onboarding case the search input is not rendered — `EmptyState` is a direct child of `PageLayout`, and the search toolbar lives only in the populated/table branch.