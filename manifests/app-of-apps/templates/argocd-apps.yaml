apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-apps
  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default

  sources:
    - repoURL: {{ .Values.repository.URL }}
      targetRevision: {{ .Values.repository.branch }}
      path: "{{ .Values.repository.baseDir }}/{{ .Values.repository.appsDir }}"
      helm:
        values: |
          {{ omit .Values "argo-cd" | toYaml | nindent 10 }}

  destination:
    name: in-cluster
    namespace: argocd

  syncPolicy:
    retry:
      limit: 5
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 30m
    automated:
      prune: true
      selfHeal: true
